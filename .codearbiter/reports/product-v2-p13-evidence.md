# P13 baseline evidence

P13 is locally accepted against the original four acceptance criteria. Delivery
is pending. The overall P-series campaign remains active.

Author: gpt-5.6-terra/high, `/root/p13_balance_preflight`. Independent reviewer:
gpt-5.6-sol/high, `/root/p13_independent_review`, separate context, final PASS with
no outstanding findings. Source baseline is 3a2f7eda7fd0738e58a716d43054d377e238e3f7.
The parent integrated the unchanged artifacts onto approved main e49b424 and
reran their deterministic and type contracts successfully.

## Result and limits

Eight scenarios and seeds 17/42 produce 16 observations. All reach GAME_OVER;
none reach the diagnostic caps. The maximum total simulation ticks is 2818 and
the maximum single settlement is 261. Four observations request inspection of
long non-damaging streaks. These flags are not a tuning verdict.

All six named mechanisms execute, with their boundaries disclosed. Team targeting
uses a controlled probe of the exported AI; it is not full-match target telemetry.
Restock uses an explicitly disclosed exhausted-stock evaluator precondition and
then an accepted real engine purchase. Siege records a real round transition and
gravity increase. Battery records the purchase, applied power above 100 and a
successful fire; Shield records accepted use; Caldera records terrain mutation.
Two buy-only preparations are classified separately from completed turns.

No gameplay, client UI, backend, dependency, telemetry or tuning change is made.
No human observations are present. Bot/scripted results do not prove enjoyment,
retention, unbiased balance, physical-device performance or player waiting time.

## Verification

- The initial absent-module check failed during setup. That was not behavioral
  RED evidence. The later controlled 2500-tick metric oracle failed after the
  long-resolution comparator was changed from `>=2500` to `>2500`; restoring the
  comparator passed the unchanged oracle. Preserve this chronology explicitly.
- Review findings corrected metric meaning, resolved defaults, accepted actions,
  policy version, neutral/incomplete iterations, real mechanism evidence, complete
  source hashing and validation of tuning observation references.
- Author and independent reviewer ran `npx --no-install tsc --noEmit -p
  scripts/balance/tsconfig.json` and `npx --no-install tsx
  scripts/checks/balance_pacing.mjs`: exit 0. Fresh generation takes about 2 seconds.
- Reviewer independently recomputed the 22-file normalized source closure and
  generated byte-identical JSON/HTML. All five artifact hashes stayed unchanged.
- Independent `npm run check`: exit 0. Independent `npm run test:client`: exit 0,
  207 files / 1,906 tests, on the pre-cleanup source baseline.
- Parent build 79c066: exit 0. Existing bundle-size advisory remains unchanged.
- Integration proof 31ca01 on e49b424: deterministic harness and script typecheck
  both exit 0,16 observations / six mechanisms. Secrets scan b663df: empty findings.
- Parent full integration `npm run check` (8d54aa, final60f9b9): exit0 on
  e49b424 plus the frozen P13 additions. Log: `product-v2-foundation-check.log`
  in the executing host's temporary directory.
- Parent checkout regression 0aab8d failed on Windows with `core.autocrlf=true`:
  Git's checkout filter changed the report from 5033 to 5044 bytes. Explicit LF
  attributes for the generated JSON/HTML fix that defect. The unchanged probe
  and the complete baseline harness passed in 7cc4e7; checkout now preserves
  all 5033 report bytes. The five frozen artifact hashes remain unchanged.
- Coverage tooling in `.codearbiter/tech-stack.md` is scoped to client Vitest:
  "`vitest run --coverage --maxWorkers=4` (v8 provider; worker bound avoids V8
  coverage oversubscription)". No coverage command covers this offline script;
  its acceptance proof is the controlled metric and invalid-record harness plus
  independent trace reduction. This is not a claimed script coverage percentage.

## Frozen artifact SHA-256

| Path | SHA-256 |
| --- | --- |
| scripts/balance/p13BalanceCorpus.ts | a05fdf7e11ddc448e6fbe1df326b4659ebc19da3009feec0f4735aa812d3adba |
| scripts/balance/tsconfig.json | 418e4e3f3e594364c06e684b85ba7ea3e2e893134f95377ee936881b06fc58af |
| scripts/checks/balance_pacing.mjs | ccc41d64916b8a88342f309adbc5232b3c1ca2cae0ff11632f58012e02b11930 |
| docs/balance/p13-v1.json | a32cb8289d54e0e9d51b21e390d678af147b289f4cad4e93e02198bc76706bc5 |
| docs/balance/p13-v1.html | c595d4fa9cca810b6c3a64f7972bbf2ab19f9d90c5840e9ea80727383414e6a8 |

Rollback removes the offline runner, check and baseline artifacts. Existing game
rules and historical verified receipts remain unchanged. Future source changes
require an explicitly versioned/reviewed baseline update; a stale pinned dataset
must not be silently accepted by changing assertions.
