# Commander Career Milestone 2 — Sprint Evidence

**Date:** 2026-08-11
**Initiative:** `career.initiative.0001`
**Branch:** `codex/commander-career-milestone-2`
**Base:** `origin/main` at `499ea46da8397049b875c71332be1a998d44d669`

## Resume and baseline

- Preserved the stale/dirty primary checkout and all pre-existing worktrees.
- Created isolated worktree `C:\Users\brenn\projects\singedTerra-worktrees\commander-career-milestone-2` from exact `origin/main`.
- `npm ci`: 0 vulnerabilities; no dependency or lockfile change authorized or planned.
- Baseline `npm run check`: PASS.
- Baseline `npm run check:edge`: PASS, 310 tests.
- Baseline client suite: PASS, 156 files / 1,378 tests (`npm run test:client -- --run`; npm emitted only its existing forwarded-flag warning).

## Recorded intent

- ADR-0012: casual client-attested XP cannot grant rank or entitlement.
- ADR-0013: the shared engine may run in one bounded verification-only Edge context; no duplicate verifier.
- ADR-0014: accepted Auth-owned Verified Deployment lifecycle, completion-time replay, and atomic verified progression.
- ADR-0016: production diagnostics are compile-time allowlisted and cannot become a generic request runner.
- `career.initiative.0001` remains in progress; tactical objectives follow verified progression becoming real.
- The malformed legacy `.codearbiter/sprint-log.md` remains byte-preserved under the initiative’s accepted governance note. This report is the slice’s append-only SMARTS and sprint evidence record.

## SMARTS decision — first product milestone after hosted feasibility

Options:

1. Award the fixed hosted probe. Rejected: it is not a player match and ADR-0015 explicitly makes it non-awarding.
2. Add tactical objectives backed by casual browser claims. Rejected for this point in the initiative: it would leave the visible rank path structurally fake and create another weak evidence stream.
3. Ship the accepted Verified Deployment lifecycle as a real fixed duel, then resume tactical objectives. Selected.

Verdict: option 3 is strong. It is the only route that is specific to the current blocked player outcome, measurable through start/replay/idempotency/rank evidence, relevant to retention, reliable under server-derived outcomes, maintainable through the one shared engine, testable at every boundary, available without removing casual play, and bounded by the already-proven hosted envelope. Confidence: high. Intent: ADR-0014 and the maintainer’s explicit approval.

## Pre-implementation adversarial architecture review

The first architecture package was BLOCKED before production implementation. No implementation file had changed; the dispatched T-01 author was stopped and confirmed no edits/tests.

Resolved High findings:

1. Browser-authored CPU commitments could manufacture easy wins. Corrected to human-only transcripts with CPU commitments regenerated by one shared deterministic Verified Duel policy.
2. The 15-action/448-tick probe fixtures were not a guaranteed gameplay envelope. Corrected to a fixed Baby Missile duel, six human salvos, at most six generated CPU salvos, deterministic cap adjudication, and an exhaustive allowed-input/worst-case combined workload gate.
3. Start retry could expire the session whose response won the race. Corrected to idempotent resume of the same unexpired session; only explicit abandon replaces it.
4. Numeric engine versions did not select executable code. Corrected with a governed deployment-drain contract: disable starts, wait the full TTL, then coordinate replay-semantic client/backend rollout; CI guards governed path/version changes.
5. Existing JSON parsing could allocate before enforcing the body limit. Corrected with a rate-limit-first bounded streaming JSON mode that counts bytes under read/cancel deadlines before parsing.

Resolved merge-blocking Medium findings:

- Engine action application gains an explicit acceptance result; the recorder no longer infers commitment from a void call.
- The bounded canonical human transcript is stored as private immutable result evidence instead of discarded.
- A versioned owner-bound local descriptor persists config/transcript after every accepted fire and supports refresh reconstruction, account-switch freeze, terminal retry, and explicit abandon.
- Diagnostics remain non-awarding and compile-time allowlisted; normal completion is not used as a diagnostic XP faucet.

SMARTS verdict: accept all nine corrections. Reliability, Securability, and Testability dominate the added scope because the rejected design could deny a legitimate award or grant one from browser-manipulated CPU play. Maintainability remains strong through one shared duel policy and explicit contracts. Available improves through resume/retry. The constrained V1 ruleset costs breadth, but no existing casual mode is removed. Confidence: high. Intent: adversarial blocker package + ADR-0014 + standing explicit approval.

## External runtime constraint

Current official Supabase documentation states a 200 ms active CPU limit for hosted Edge Functions. V1 retains a conservative 100 ms local replay target, but the Verified Duel must prove its shared hard tick bound across every allowed single-salvo seed/angle/power path and measure adversarial full-duel sequences; the older 448/198 probe fixtures remain runtime diagnostics only and are not treated as gameplay limits.

## TDD ledger

Pending. Every production behavior will receive a causal RED entry before implementation and a GREEN plus mutation result afterward.

## SMARTS decision — exact Verified Duel envelope

Options:

1. Quantize aim/power to a small artificial grid. Rejected: it simplifies the oracle but makes the career duel feel unlike the game and reduces player agency without improving the hard engine bound.
2. Claim exhaustive coverage of every six-human-shot sequence. Rejected: the Cartesian sequence space is intractable and the claim would be false confidence rather than a useful gate.
3. Preserve native integer controls, exhaust every legal single-salvo tuple across a finite server seed pool, derive the full-duel ceiling from shared hard constants, and exercise an adversarial multi-salvo corpus. Selected.

Frozen player contract: seeds `[17, 42, 73, 109]`; angle `0..180`; power `0..100`; Baby Missile only; six human and at most six regenerated bounded-CPU salvos; fixed two-seat/open/no-hazard/one-round options. The corrected CPU uses an exact 35-coarse + 25-refinement shared search with Baby Missile forced and disallows every purchase, movement, shield, and alternate weapon.

Read-only design measurements before TDD:

- Sample grid `[17,42,73,109] × angle 15..165 step 5 × power 20..100 step 5`: 2,108/2,108 settled; maximum 186 ticks.
- Full frozen domain `[17,42,73,109] × angle 0..180 × power 0..100`: 73,124/73,124 settled inside the 391-tick hard bound; measured maximum 188 ticks; exploratory harness elapsed about 8.3 seconds for the entire domain.
- The shared hard constants are 240 flight ticks, 600 terrain rows, and 4 settle pixels/tick. Including the convergence tick gives 391 ticks/salvo and 4,692 ticks for 12 salvos.
- Twenty-four adversarial repeated-shot duels across all four seeds reached at most 1,764 ticks and about 24 ms locally; early terminal and cap paths both occurred.
- The initially proposed medium AI planner averaged about 1.57 ms over 2,108 post-human-shot states with a measured maximum about 6.36 ms, but its 237-probe hard envelope was omitted from the first bound. The second adversarial review correctly rejected that policy despite favorable exploratory timing.

SMARTS verdict: option 3 is strong. It is Specific and Measurable through exact domains and shared constants; Relevant and Available because controls match ordinary play; Reliable and Testable through exhaustive per-salvo plus adversarial sequence evidence; Maintainable because the oracle imports the engine caps instead of duplicating them; Securable because no browser-authored CPU or config enters the transcript. Confidence: high. Intent: adversarial architecture correction plus explicit maintainer approval.

## SMARTS decision — bounded request limits

Selected limits are 1,024 UTF-8 bytes for exact completion JSON, 128 bytes for exact abandon JSON, a 2-second body-read deadline, and a separately bounded 250 ms cancellation attempt. The strongest counter-consideration is future transcript growth, but contract-version changes already require deployment drain; silently reserving a large body today weakens the allocation boundary for no V1 player value. The six-entry canonical transcript plus UUID fits with substantial structural headroom. Verdict: strong across Specific, Measurable, Reliable, Securable, and Testable; future versions must deliberately revise the exact contract. Confidence: high. Intent: adversarial unbounded-body finding plus standing approval.

## Second pre-implementation adversarial architecture review

Verdict: BLOCK. No production implementation had begun. Accepted findings:

1. HIGH: per-IP rate limiting did not satisfy ADR-0014's required per-account boundary.
2. HIGH: the 4,692 live-tick ceiling omitted medium-AI probe simulation and sweep work.
3. HIGH: deployment drain lacked an executable persisted switch, authoritative final-start time, zero-active assertion, and rollout order.
4. Merge-blocking MEDIUM: the 30-minute expiry promise contradicted unconditional recovery/retry language.

SMARTS corrections:

- Retain coarse pre-Auth IP defense, then authenticate and apply a fail-closed Auth-derived 10/min account bucket before body consumption or replay. Same-account/multiple-IP tests prove the authority; completed same-evidence retries return stored receipts without replay.
- Replace the general medium planner with an exact two-pass Verified CPU search: at most 35 fixed coarse candidates plus 25 refinements, 60 unique probes. Six CPU turns therefore add at most 86,400 simulated physics ticks. Fixed V1 velocity/drag/wind/gravity keep one-pixel sweep below 24 samples per simulation tick, capping CPU swept collision work at 2,073,600 samples. Maximum-probe and six-turn workloads join the local and hosted non-awarding gates.
- Persist `starts_enabled`, `disabled_at`, and `last_started_at`; add service-only control/status RPCs plus checked-in credential-safe `disable/status/enable` tooling. Readiness is refused until `last_started_at + 30 minutes` and zero unexpired sessions. Old completion/abandon stay live during the wait; backend precedes strict-version client; enable follows production verification.
- Keep the 30-minute bound but state the truth: retry is only guaranteed before expiry. HUD countdown warns at five and one minutes; expiry freezes verified input and requires explicit continuation as casual or battery return. No expired play earns rank.

Verdict: accept all four. The added operational and UI scope is justified by Reliability, Securability, Availability, and Testability; the cheaper CPU policy also improves Maintainability. The strongest counter-consideration is that expiry can disappoint an idle player, but pretending an expired award remains recoverable would be worse and cannot be enforced without live writes. Confidence: high. Intent: fresh adversarial review + explicit maintainer approval.

## Second-review correction loop

The adversary retained BLOCK for two task-ownership defects, not architecture defects:

1. HIGH: the required hosted maximum-probe/full-duel proof had no implementation owner. Corrected by adding the existing `verified_replay_probe`, its immutable fixture/workload tests, and the exact diagnostics response validator to T-03 with causal RED/GREEN obligations. The endpoint remains no-body and non-awarding.
2. HIGH: T-01's drain oracle demanded old-handler availability and strict client refusal before those handlers/client existed. Corrected by limiting T-01 to schema/control/readiness, credential-safe CLI, and rollout manifest. Disabled-start plus old resume/abandon/complete integration moved to T-04; strict client-version refusal moved to T-06; ordered production proof remains T-09.

SMARTS verdict: accept both corrections. They preserve the same player/security outcome while restoring task dependency truth and causal testability. Confidence: high. Intent: adversarial correction loop.

Architecture closure: PASS on the corrected live package. The adversary confirmed all six blockers resolved and modified no files. Production implementation may now begin under the frozen spec/plan.

## T-01 implementation and review

- Causal RED captured for absent migration 016, absent drain CLI, and absent npm gate wiring.
- Initial GREEN: both focused oracles, required in-memory mutations, Node syntax checks, `git diff --check`, and full `npm run check` passed. Live PostgreSQL 15 execution remains mandatory at the migration/deployment gate because Docker/psql were unavailable.
- Controller review corrected the historical-migration path set, conflicting-evidence model, drain fallback clock, missing bounded summary RPC, caller-owned drain time, transcript/award invariants, PostgreSQL-15 JSON key counting, and oversized-number parsing before task acceptance.
- Fresh task review returned BLOCK: HIGH account deletion was prevented by cascade/restrict plus unconditional delete triggers; merge-blocking MEDIUM findings covered unvalidated drain RPC success, staged historical-migration edits escaping the oracle, and missing `user_id` summary index. All four were returned to the implementer.
- Correction review: PASS. Account deletion now cascades while operational mutation remains guarded; the drain CLI validates exact RPC rows and contract state; both working-tree and staged historical-migration mutations are rejected and causally fixture-tested; progression reads have a `user_id, created_at` index. Numeric transcript casts remain `::numeric`.
- Controller verification: focused migration and drain oracles passed, and a fresh integrated `npm run check` completed with exit code 0. T-01 is ACCEPTED. Live PostgreSQL 15 execution remains a mandatory deployment gate because no local PostgreSQL/Docker runtime was available.
- T-02 is IN_PROGRESS under its frozen task brief; causal bounded-transport, contract, and handler RED evidence precedes implementation.
- SMARTS parallelism decision: T-03 begins alongside T-02. The tasks have disjoint production write sets and converge only at T-04, so concurrency is Available and Relevant while remaining Reliable/Testable through separate reports and task reviews. Strongest counter-consideration: shared full-suite churn; controller integration checks remain serialized after both task acceptances. Confidence: high. Intent: standing approval for the durable testing interface and longer-horizon delivery.
- T-02 author concern exposed a same-millisecond resume ambiguity. Corrected migration 016 to return authoritative `resumed`, extended the migration oracle/model/mutations, and removed handler inference. Fresh review then BLOCKED on a HIGH fail-open projection: start/abandon accepted RPC rows without checking returned `user_id` against the Auth-derived owner. Missing/malformed/foreign-owner RED tests, exact equality guards, identifier-safe failures, and ownership-removal/inversion mutations closed it.
- T-02 correction review: PASS. Focused 26/26, migration/drain oracles, and Edge 335/335 passed; direct foreign-owner probes now fail generically and disclose no owner/session identifiers. T-02 is ACCEPTED.
- T-03 initial task review: BLOCK. One short nonterminal transcript could be externally adjudicated into an awardable result (CRITICAL); the shared live controller was absent (HIGH); workload bounds were repeated rather than derived/exercised (HIGH); exact CPU/adjudication mutation coverage and report path inventory were merge-blocking MEDIUM. All five returned to the author; T-03 remains IN_PROGRESS.
- T-03 second review confirmed the original CRITICAL closed but BLOCKED controller/engine identity mismatch, a still-unproved swept-collision ceiling, and parity tests that manually ticked the controller instead of exercising browser rAF. Corrected with canonical/private construction, controller-derived HotSeat engine identity, analytic bounds from exported physics constants, mocked-rAF integration tests, and causal identity/physics/rAF mutations.
- T-03 final correction review: PASS. Short nonterminal transcripts reject; legitimate early terminal and six-salvo outcomes pass; live browser/controller and verifier bytes agree; hosted diagnostics remain no-body/storage-free/non-awarding. Fresh harness 73,124, client 1,386, Edge 335, full `npm run check`, and diff-check passed. T-03 is ACCEPTED.
- T-04 is IN_PROGRESS under its frozen completion brief. It joins only accepted session, bounded-transport, and shared-verifier interfaces; exact state/policy/race tests must fail before implementation.
- T-04 causal RED exposed an upstream interface gap before production edits: all private table reads are correctly revoked, while no non-mutating owner-bound RPC returns the session descriptor and optional immutable result needed to validate state/versions and same-evidence retries before replay. Calling the award RPC as a lookup would violate replay-before-mutation.
- SMARTS correction: narrowly reopen T-01 for `verified_deployment_completion_context(user, session)`, a service-only stable SECURITY DEFINER read returning exactly one owner-bound session descriptor plus nullable immutable result evidence and performing no mutation. This is Specific/Measurable through an exact oracle; Relevant/Reliable because it restores the required ordering; Available/Maintainable because it extends migration 016 before deployment; Securable because direct table grants remain revoked; Testable through owner/mutation/ACL models. Strongest counter-consideration: reopening an accepted task, but preserving a known-invalid interface boundary would be worse. Confidence: high. Intent: standing bounded-spec approval.
- Reopened T-01 final review: PASS after iterative hostile mutation hardening. The completion-context RPC is owner/session-bound, non-mutating, fixed-search-path, service-only, and exact-shaped. The migration oracle now fail-closes on equivalent routine signatures, quoted identifiers, broad/schema grants, role membership, grant options, routine/schema/table lifecycle changes, replacement/drop/revoke ordering, dynamic DDL, and unknown top-level statements; exact database type assertions are wired into the aggregate gate. Migration/drain, Deno, Edge 335/335, full `npm run check`, and diff checks passed. Live PostgreSQL 15 execution remains mandatory at deployment.
- App-restart checkpoint: T-01/T-02/T-03 ACCEPTED; T-04 production code not started and is ready to resume from task-4-brief.md. No commit, staging, PR, migration execution, or deployment has occurred in this slice.
- Post-restart recovery verified the isolated branch through Windows Git because the WSL shell cannot resolve Windows-form linked-worktree metadata. The persisted diff was intact; fresh `npm run check` exited 0 and `npm run check:edge` passed 335/335. T-04 is IN_PROGRESS from its existing causal RED and newly accepted completion-context seam.
- Second app-restart checkpoint: the T-04 author completed the bounded implementation and reported Edge 348/348, full aggregate checks, diff-check, and five causal mutation probes restored. The assigned adversarial reviewer did not return a verdict after two bounded waits and an explicit finish-now request; it was closed as stuck together with the completed author. This is not acceptance: T-04 remains IN_PROGRESS/review-pending, T-05 has not begun, and the next run must perform a fresh adversarial T-04 review that includes the author's concern about missing generated RPC declarations for `complete_verified_deployment` and `verified_progression_summary`. No commit, staging, PR, migration execution, or deployment occurred.
- T-04 fresh review: BLOCK on one merge-blocking Important finding. The authoritative migration defines `complete_verified_deployment` and `verified_progression_summary`, but the exact shared database function type map/test omit them and the handler substitutes a local RPC interface. SMARTS verdict: correct the shared contract in T-04 fix round 1. Reliability, Securability, Maintainability, and Testability all favor one canonical typed RPC surface; preserving the task's original path boundary would institutionalize drift. Strongest counter-consideration: this reopens two accepted T-01 files, but the correction is additive, test-first, and does not alter SQL/runtime behavior. Strength: strong; confidence: high; intent: per approved exact-contract sprint requirements and standing autonomous authority.
- T-04 correction and acceptance (2026-08-12): causal type-test RED caught both missing RPCs and the closed key-set mismatch. Exact shared Args/Returns, key/contract assertions, and `Database`-grounded handler dependencies then passed 27 focused tests, Edge 348/348, the full aggregate check, mutation removal of `created_at`, and diff-check. Scoped adversarial re-review marked the sole finding ADDRESSED with no new Critical/Important breakage. The controller independently reran the 27-test focused matrix green. T-04 is ACCEPTED; T-05 is IN_PROGRESS.
- T-05 initial implementation (2026-08-12): author reported Edge 23/23, client 68/68, full Edge, client 1394/1394, aggregate checks, and required mutation families green. Controller independently confirmed Edge 23/23 and client 68/68 (using the client-workspace-relative Vitest filter after the plan's repository-relative filter correctly matched no file). Fresh review BLOCKED on one Important mismatch: runtime requires exact `verifiedProgression`, but the exported `AccountSummary` type marks it optional. SMARTS verdict: make the field required and update every legitimate fixture/constructor. Reliability, Maintainability, Securability, and Testability favor compile-time alignment with the fail-closed runtime boundary; compatibility cost is bounded to internal fixtures/constructors. Strength: strong; confidence: high; intent: per approved required-exact verified authority.
- T-05 correction and acceptance: causal type RED and optionality mutation pinned required `verifiedProgression`; all legitimate account/session/UI fixtures now supply exact verified values, and account UI no longer reads casual fallback totals. Focused 84/84, full client, aggregate checks, and scoped adversarial re-review passed with no new Critical/Important breakage. Controller independently reran 84/84 green. T-05 is ACCEPTED; T-06 is IN_PROGRESS.
- Incremental player steer (2026-08-12): victory presentation currently appears too quickly to enjoy the terminal opponent blast. SMARTS verdict: incorporate this into T-07's final presentation composition as a deterministic post-impact beat, not T-06 recovery plumbing. Satisfaction and Reliability strongly favor sequencing the overlay from terminal impact completion; Maintainability and Testability favor one explicit state transition with reduced-motion and duplicate-input coverage. Strongest counter-consideration: a fixed delay can feel sluggish, so the beat is short and event-tied. Strength: strong; confidence: high; intent: explicit current player feedback.
- T-06 initial implementation and review (2026-08-12): author reported 36 causal RED failures, 154 focused GREEN tests, full client 1444/1444, typecheck/check, and seven mutation families killed/restored; controller independently confirmed 154/154. Fresh adversarial review BLOCKED on two Important lifecycle defects: time captured before awaited completion/abandon could leave a post-expiry failure retryable, and account-switch freeze could never recover when the rightful owner returned. One Minor UUID variant/version validation gap is cheap and aligned with fail-closed exactness, so it is included. SMARTS verdict: resolve all three in fix round 1. Reliability, Securability, Satisfaction, and Testability dominate; owner return requires a fresh server descriptor exactly matching local immutable recovery before unfreeze. Strength: strong; confidence: high; intent: per approved recoverable lifecycle and exact-contract requirements.
- Quick Duel seed diagnosis (2026-08-12): production `Lobby.startQuickDuel()` emits no `settings.seed`; hot-seat `createClient()` then intentionally omits seed, and `GameEngine` falls back to fixed `DEFAULT_SEED`. This exactly explains the same two attacks winning across browsers/redeployments. The verified endpoint is not the cause: it selects and persists a server-owned seed from its accepted set. `$ca-debug` exits to a bounded T-07 test-first fix. SMARTS verdict: inject one fresh browser-generated unsigned 32-bit seed per casual Quick Duel launch, with an injectable generator for deterministic causal tests; never mutate seed on verified retry/resume. Satisfaction and Reliability strongly improve while replay/debug tests remain deterministic through injection. Strength: strong; confidence: high; intent: explicit player reproduction plus cited data flow.
- T-06 correction and acceptance: ten causal correction failures pinned deferred completion/abandon crossing expiry, frozen evidence preservation, exact rightful-owner fresh resume matching/mismatch/error paths, and canonical UUIDv4 variant/version. Focused 163/163, full client 1453/1453, typecheck/check, four correction mutations, and scoped adversarial re-review passed with no new Critical/Important breakage. Controller independently reran 163/163 green. T-06 is ACCEPTED; T-07 is IN_PROGRESS.
- T-07 implementation and acceptance: causal lobby/HUD/main RED preceded composition. Author reported 96 focused, 1475 full client, 14 browser checks, typecheck/build/check, and 10/10 mutation families green; controller independently reran 96/96. Initial adversarial review correctly refused to trust a compact package that omitted two load-bearing excerpts; this was an evidence-package correction only. The supplemented re-review verified browser-entropy uint32 Quick Duel seeds exactly once per click with distinct injected redeployments, immutable verified seed routing, renderer-settled terminal impact notification, one 420ms/120ms readable payoff beat, and inert duplicate-navigation suppression. No new breakage. T-07 is ACCEPTED; T-08 is IN_PROGRESS.
- T-08 initial adversarial review BLOCKED on two High evidence gaps: the payoff fixture had no real rendered explosion, and the browser package omitted refresh/warnings/expiry/retry/award/promotion/failure lifecycle proof. Both findings were accepted. The correction pass injected one authoritative terminal explosion through the production renderer and added the full authenticated lifecycle matrix. The new browser tests exposed three real integration defects—persistent renderer dressing could suppress recovered terminal reports forever, hidden receipt CSS still rendered an empty box, and repeated expiry updates stole keyboard focus—and fixed each under causal RED/GREEN evidence. The corrected matrix passed 27/27 across desktop/touch/small-window; full browser passed 292 with 29 intentional skips; full client passed 1475/1475; aggregate check and diff-check passed. A zero-delay mutation failed at about 4.4ms versus the 400ms floor and was restored. T-08 remains IN_PROGRESS pending fresh adversarial correction review.
- T-08 acceptance: fresh adversarial correction review returned PASS with no Critical, High, or other merge-blocking findings. It explicitly cleared both original High findings, independently reran the 27/27 browser matrix, confirmed Quick Duel's per-deployment browser uint32 seed without weakening server-owned verified seeds, confirmed real explosion → impact completion → 420ms/120ms inert payoff ordering, and verified all five mutation targets restored. T-08 is ACCEPTED; T-09 final governance and delivery is pending.
- T-09 fresh local gate run (2026-08-12, Windows host): dependency audit PASS with 0 vulnerabilities; Edge PASS 351/351; client PASS 1,475/1,475 across 158 files; aggregate static/determinism checks PASS; production build PASS; migration and drain contract oracles PASS; full Playwright PASS 292 with 29 intentional profile skips. Quick Duel browser coverage explicitly proved distinct query-gated seed receipts on consecutive redeployments while verified deployment retained its immutable server-owned seed.
- Coverage audit (2026-08-12, Windows host): 95.35% lines and 84.91% branches, clearing the stage-1 60% floor for both required metrics. Statements were 93.10% and functions 90.13% for context. No coverage exemption is used.
- Exact working diff passed `git diff --check`. The unrelated untracked `NUL` artifact remains preserved and excluded from this slice; no destructive cleanup authority is inferred.
- Final holistic review round 1: BLOCK with no Critical or High findings and two merge-blocking Medium findings. A stale local account summary could suppress an already-authoritative award, and a refreshed account B deployment could overwrite account A's recoverable transcript. One Low finding identified a probabilistic Quick Duel E2E inequality assertion. All three findings were accepted.
- Correction round 1 used causal RED before production changes. Three focused client tests failed for stale-summary, null-summary, and cross-account storage loss. Server RED proved the completion RPC omitted immutable result-specific progression fields. The correction persists exact prior/current progression in the same per-account completion transaction, returns it unchanged on same-evidence retry, accepts a refreshed client summary that contains the immutable result even if another device has advanced farther, and stores up to four bounded owner-session recovery records without persisting account identity. The E2E probe now proves one bounded seed receipt per launch without rejecting a valid CSPRNG collision.
- Correction verification: 14/14 focused Edge/type tests; all 48 migration mutations; 148 focused client tests plus the explicit A to B to refresh to B activity to A recovery integration; Edge 352/352; client 1,478/1,478; aggregate checks; production build; migration oracle; coverage 95.28% lines and 84.84% branches on Windows; focused verified browser 27/27; isolated aim-guide browser 2/2; final full browser 292 passed with 29 intentional profile skips. The first full browser attempt exposed six stale storage-v1 fixtures and one transient aim-guide pixel failure; fixtures were updated, the aim test passed isolated, and the exact final full rerun passed.
- Non-blocking review observations were persisted through `$ca-task`: complete service-only generated database typings, and directly assert unused legacy GameEngine action-acceptance returns before any verified consumer depends on them.

## T-09 production-start correction (2026-08-12)

- Authenticated production reproduction: selecting `Start verified deployment` showed the generic unavailable state while Supabase recorded the corresponding `POST start_verified_deployment` invocation as HTTP 200. This refuted disabled admission and handler failure.
- Root cause: the Edge response returned the raw PostgreSQL `timestamptz` spelling, while the client accepted only byte-identical `Date#toISOString()` text. A valid UTC-offset deadline such as `2026-08-12T13:30:00+00:00` therefore caused the exact start response to be discarded before storage or launch.
- SMARTS compared server-only output rewriting, permissive raw timestamp retention, and client boundary canonicalization. Client canonicalization is selected with strong/high confidence: it accepts the standards-equivalent database response already in production, emits one stable UTC representation for lifecycle/storage equality, changes no Auth or database authority, requires no backend deployment, and remains fail-closed for non-string or unparseable input.
- Causal RED: the new real-parser regression failed alone with `expected undefined to be '2026-08-12T13:30:00.000Z'`; the other 24 contract tests passed. GREEN: the focused contract file passed 25/25 after the minimal canonicalization.
- The full sweep exposed two date-fixture tests that crossed their fixed August 12 expiry through real `Date.now()` after an await. Their existing `afterEach` timer restoration made a narrow per-test fake clock safe; the corrected focused client matrix passed 78/78 and full client passed 1,487/1,487.
- The Windows aggregate gate also exposed CRLF-sensitive mutation replacement in the verified-deployment migration oracle. Normalizing the read-only fixture text to LF made the same 48 hostile mutation checks portable without changing SQL or production behavior.
- Fresh local verification: focused client 78/78; full client 158 files / 1,487 tests; Edge 352/352; `npm run check` PASS; production build PASS; dependency audit 0 vulnerabilities; migration oracle PASS; `git diff --check` PASS.
- Adversarial review round 1: BLOCK with Critical 0, High 0, two merge-blocking Medium findings, and Low 0. Unrestricted `Date.parse` admitted host-dependent, zone-less, rollover, and non-contract inputs; the correction receipt was also absent from the canonical sprint log and the untracked review-package scope was not self-reproducing. Both findings were accepted.
- Correction round 1: eight hostile timestamp cases failed causally, covering date-only, zone-less, locale, RFC-1123, calendar/hour rollover, invalid offsets, and excessive precision. The closed grammar requires a four-digit modern year, `T`, seconds, explicit `Z` or `±HH:MM`, at most PostgreSQL microsecond precision, valid calendar/time fields, and an ISO offset no larger than 14:00; valid zero/nonzero offsets canonicalize to UTC. Focused parser GREEN is 35/35. The SMARTS receipt is appended to `.codearbiter/sprint-log.md`, and the review package now declares both the tracked diff and the explicit untracked file content/hash command.
- T-09 delivery and production acceptance (2026-08-12): corrected exact head `dc5c5fc04a8dc291dbc08d12d74aa144ea1b46a4` cleared adversarial, architecture, and coverage review with no merge blockers. PR #407 was squash-merged as `main@0336e18beb189041d01f0754107064a821bc79b7`. Post-merge CI run `31650838445`, CodeQL run `31650838460`, and Pages run `31650838432` all passed on that exact main SHA; Pages also passed current-main provenance verification and its live render smoke.
- Authenticated production proof used the browser-managed Commander SUaDtL session at `https://suadtl.github.io/singedTerra/`. `Start verified deployment` launched successfully with a fresh `29:59` deadline, proving the valid `+00:00` server timestamp now crosses the client trust boundary. The real duel completed at the six-human/six-CPU salvo cap with Commander health `100` versus CPU health `63`; the After Action Report displayed `Verified victory · +200 XP`, and returning to deployment preparation showed the durable dossier change from `500 XP to Level 2` to `300 XP to Level 2`. No Supabase change belonged to the timestamp correction; the already-deployed verified functions and schema remained authoritative.
- Closeout review correctly BLOCKED the initial T-09 acceptance claim: the production receipt above proves fresh launch, one real cap completion/award, summary/AAR, and durable dossier change, but does not yet prove same-owner resume identity, account limiting, refresh recovery, completion retry/idempotency without replay, expiry presentation/freeze, or casual fallback. The plan now separates proved and outstanding obligations and T-09 remains IN_PROGRESS.
- `First Strike` (damage the CPU within the first three human salvos) remains the strongest SMARTS candidate for the tactical-objective milestone because it directly teaches aim/power/wind adjustment without adding reward or persistence authority. Selection and transition are explicitly deferred until the remaining T-09 production lifecycle matrix passes. `career.initiative.0001` remains active.
- Authenticated recovery proof (2026-08-12): a second production deployment recorded one real human and one regenerated CPU salvo, leaving Commander health `100`, CPU health `67`, and `29:33` remaining. After a full page reload and normal splash/local-battle navigation, the same `Start verified deployment` route resumed at `You 1 / 6 · CPU 1 / 6`, the same `100`/`67` health, and the continuing `29:14` deadline. It did not allocate a blank deployment. This proves same-owner identity binding, browser refresh recovery, and the server-enforced one-active-session-per-account path through the player UI.
- The recovered deployment then completed normally at five human/five CPU salvos with Commander health `67`, CPU health `0`, `Verified victory · +200 XP`, and `100 XP to R-02 Gunner`. A second full reload showed the durable dossier at `100 XP to Level 2`. This strengthens completion and persistence proof but does not claim a forced lost-response retry, duplicate completion replay avoidance, or expiry presentation.
- At the recovery checkpoint, the remaining T-09 hard/temporal boundaries were anonymous casual fallback, a real 30-minute expiry, and controlled lost-response completion retry/idempotency. The later receipts below close the first two without mutating the authenticated session or clock; forced production fault injection remains open.
- Anonymous casual-fallback proof (2026-08-12): a separate clean in-app browser context loaded production with only the anonymous `Account` entry point and no Commander dossier or verified-progression claim. After dismissing the splash, `Quick Duel vs CPU` launched a normal playable match as `Player 1` versus `CPU 1`, with ordinary controls and no Verified Deployment status. The signed-in Chrome session remained untouched. This closes the anonymous casual-fallback obligation without crossing the auth mutation gate.
- Temporal proof launch: the authenticated production browser started a new untouched Verified Deployment at `30:00` on 2026-08-12 19:44 EDT and left it open for the real deadline; no clock, database row, or server response was altered to accelerate the result. The resulting expiry receipt appears below.
- Completion retry/idempotency evidence boundary: two successful production completions each emitted one completion POST and one award, while the exact automated stack proves terminal evidence retention/retry before expiry (`Lobby.account.test.ts`), retry/freeze composition (`main.hotSeatProgression.test.ts`), completed same-evidence immutable receipt without replay (`complete_verified_deployment/index.test.ts`), and the locked atomic stored-result path plus conflict rejection (`verified_deployment_migration.mjs`). A forced production response loss *after* the server commit cannot be created through the ordinary player UI. Doing so would require authenticated browser-runtime interception, a proxy/network control, or production infrastructure mutation. Those are security/operational hard gates under the standing authorization, so no such manipulation is performed or claimed; the production fault-injection obligation remains open pending explicit user authorization or a naturally occurring retry.
- Honest expiry receipt (2026-08-12): the untouched authenticated session transitioned from ordinary active state to `Five minutes remain` at `04:41`, then `One minute remains` at `00:13`, while the command deck remained available before the deadline. At `00:00` it displayed `Verification expired.`, disabled `Retry verification`, contained the battlefield behind a modal, and offered exactly `Continue casually` and `Return to Battery`. Choosing `Return to Battery` returned to signed-in deployment preparation with the dossier still at `100 XP to Level 2` and a fresh `Start verified deployment` action. No salvo, award, clock change, row mutation, or claim about the unchosen casual-continuation path was made.

## T-09 completion-retry diagnostic correction (2026-08-13)

- The prior security/operational hard-gate classification was wrong and is superseded. Discarding one already accepted completion response changes no authentication, authorization, identity, credentials, secrets, crypto, or trust-boundary control. The user's standing approval covers this bounded production diagnostic, and execution remains the agent's responsibility.
- SMARTS selected extension of the existing authenticated Production Diagnostics toolkit over browser injection or a parallel ad-hoc hook: S=5, M=5, A=5, R=5, T=5, Satisfaction=5, confidence high. The toolkit already owns explicit operator gating, secret-free projection, lifecycle invalidation, timeout handling, and responsive modal containment.
- The inert one-shot tool arms only while authenticated, discards only after the ordinary adapter exact-parses and accepts a completion response, and then lets the existing retained-evidence Retry verification path resubmit. PASS requires the identical session/transcript evidence, byte-identical immutable receipt, one match delta, outcome-correct win delta, and exactly the receipt's 100/200 verified-XP delta. A bounded secret-free terminal result persists for same-tab reload inspection.
- The same toolkit gains a reusable live-game diagnostic: a credential-omitting, no-store, same-origin read of exact bounded `deploy-meta.json`, reporting only the deployed 40-character SHA and decimal Pages run ID. No operator endpoint, body, header, credential, or arbitrary fetch input exists in the UI.
- Causal RED/GREEN covered ordinary-mode inertia, one response discard, identical retry and award deltas, mismatch failure, adapter integration, exact provenance fetch/shape, timeout timer cleanup, stale-generation isolation, view states, and rendered control reachability. Adversarial correction added causal auth-isolation RED for non-auth and direct account-identity transitions, active-proof rearm refusal, exact-key/arithmetic persisted-proof validation, hostile persisted deltas, and real same-tab module hydration. Coverage correction then proved same-evidence/different-receipt failure persistence plus the exact 10-second Pages timeout boundary, timer cleanup, and late-response isolation. Architecture correction scrubs private session/transcript/receipt material at every terminal transition, aborts timed-out provenance transport, and stream-counts UTF-8 bytes before allocating no more than 4,096 bytes. Multibyte overflow and exact-4,096-byte acceptance prove the boundary counts transport bytes and uses a strict greater-than limit. Corrected fresh gates: client 1,529/1,529; typecheck/build PASS; aggregate deterministic/static checks PASS; Edge 352/352; dependency audit 0 vulnerabilities; diagnostics Playwright 12/12; diff-check PASS.
- H-05 recovery: Windows PowerShell line endings made the attempted canonical sprint-log append appear to rewrite the malformed historical file. Under the logged routine override, the committed sprint log was retained byte-for-byte and this UTF-8 milestone report remains the slice-specific SMARTS record, matching the repository's existing malformed-log disposition.
- Fairness follow-on SMARTS: S=5, M=5, A=5, R=5, T=5, Satisfaction=5, confidence high. Player evidence shows Quick Duel two-shot wins, the verified CPU landing exact hits each turn, and even an Easy online CPU first-shot direct hit through 6 mph wind. The correction must diagnose generic Easy aim, the verified-duel aim oracle, decisive starter damage, and spawn geometry together and deliver a recoverable multi-turn duel rather than merely swapping which side wins in two salvos.
- Responsive-composition follow-on SMARTS: S=5, M=5, A=5, R=5, T=5, Satisfaction=5, confidence high. Add cross-viewport no-document-scroll lobby invariants with explicit intentional-scroller allowlists, and establish a battlefield HUD safe zone so tank spawns cannot be hidden behind the command deck.

## T-09 deployed diagnostic and operator-lifecycle correction (2026-08-13)

- PR #409's exact reviewed head `99dd64c47ec40196d7bbe641afb3aaeb33eac52b` cleared CI, CodeQL, and browser rendering guardrails, then squash-merged as `main@3f067eb0fcac37095897ddbc7869a2322668fc68`. Exact-main CI, CodeQL, Pages deployment provenance, and Pages live render smoke passed. No Supabase migration, function, or secret change was associated with this client-only deployment.
- Authenticated production inspection confirmed the gated Production Diagnostics panel, the deployed completion-retry arm state, and the existing in-progress deployment. It did not complete a deployment, inject a response loss, or claim a retry receipt. Reloading before completion safely returned the module-local armed state to inert idle.
- Live panel use surfaced a recoverability gap: after arming, closing the panel to play left no visible way to withdraw the one-shot fault before a response was affected. SMARTS selected an explicit toolkit-native cancel control rather than a hidden reload instruction: S=5, M=5, A=5, R=5, T=5, Satisfaction=5, confidence high. The control is permitted only while `armed`; once a response is discarded the evidence must remain for the ordinary retry proof. Sign-out/account-change cancellation remains separately capable of clearing an active discarded proof. Causal RED covers both the forbidden post-discard cancellation and disabled control; GREEN requires arm, cancel, inert state, and re-arm through the real Lobby wiring. This correction is review/CI/deploy/live-proof pending, so T-09 remains IN_PROGRESS and no completion-retry production receipt is claimed.

## T-10 Easy CPU recovery correction (2026-08-13)

- Observed player harm: Easy uses the same exhaustive wind-correct ballistic search as tougher CPU seats, then applies only a small seeded perturbation. That permitted a first-show direct hit through 6 mph wind and made an ostensibly Easy online duel feel solved before the player could learn from it.
- SMARTS compared merely enlarging random-looking jitter, weakening all CPU difficulties, and constraining only Easy's first response after the human opener. The third option is selected: S=5, M=5, A=5, R=5, T=5, Satisfaction=5, confidence high. It preserves deterministic lockstep and Medium/Hard competence, gives a fresh player an observable wind lesson and room to answer, and restores Easy threat on later turns rather than converting the match into a harmless CPU.
- Causal RED: `npx tsx scripts/checks/ai.mjs` now executes a real human `p1` fire/settle before Easy `p2` responds, across both -6 and +6 mph matrices. Before the correction, first-response damage reached 28.10; deliberately restoring the flawed health-only gate also made the later-threat assertion fail at zero. Both failures were observed before restoring the turn-bound rule.
- GREEN: the shared planner now chooses the nearest deterministic, simulated aim candidate outside a 60px target-center miss distance only for Easy during the duel's first two global turns. The real player-to-CPU response matrix's worst damage is 0 and it observes four later damaging responses; `npm run typecheck` passes. This is local implementation evidence only; aggregate checks, adversarial review, hosted CI, deployment, and live play proof remain required before any delivery claim.

## T-11 visual containment correction (2026-08-13)

- Root-cause receipt: the online lobby card itself declared `overflow-y: auto`, so any composition drift reintroduced a scrollbar around the entire fixed stage. The fine-pointer command deck was a 236px upper-left battlefield overlay while deterministic two-seat games spawn at 10%/90% horizontal stations; tall terrain could therefore put the left tank entirely behind it.
- SMARTS selected a narrow composition correction instead of changing tank placement, adding a new overlay, or weakening the online flow: S=5, M=5, A=5, R=5, T=5, Satisfaction=5, confidence high. Keeping the command deck interactive while placing it in the left-bottom free lane preserves shared lockstep spawn semantics; making the fixed lobby non-scrollable prevents the recurring outer scrollbar without changing intentionally scrollable dense sub-surfaces.
- Causal RED: the browser regression matrix first failed because the deck intersected the left 10% spawn column and the online card computed `overflow-y: auto`. Adversarial review then correctly blocked the initially centred replacement because it covered the supported three-seat 50% station. The corrected oracle covers the full two-/three-/four-seat centre matrix (10%, 15%, 36.67%, 50%, 63.33%, 85%, 90%) plus the widest shipped 36px chassis footprint; it failed against the centred deck and is GREEN with the deck in the left-bottom free lane (x=200..420). The online card computes `overflow-y: hidden`. Fresh rendered production-bundle matrix: 58 passed, 20 pointer-specific skips across desktop-fine, Pixel 5 landscape touch, and small fine-pointer viewports. Re-review, hosted CI, merge, Pages deployment, and production geometry proof remain outstanding.

## T-09 retry-report reachability correction (2026-08-13)

- Fresh authenticated production exercise reached the actual Verified Deployment terminal path at the six-human/six-CPU salvo cap with `24:17` remaining. The armed diagnostics seam produced the expected `Verification needs another attempt.` state, proving the first accepted completion response was deliberately discarded after server acceptance. This is not a completion-retry receipt: the ordinary retry remains unproved until it returns an immutable receipt and award.
- Root cause of the failed player retry was visual and reproducible: `Retry verification` rendered in the command HUD behind the blocking After Action Report. Two visible activation attempts left the state retryable; no retry POST or receipt is claimed. The component-level retry callback existed, but the modal intercepted the pointer path.
- SMARTS selected a conditional in-report action over removing the report, making the report non-modal, or adding another diagnostic surface: S=5, M=5, A=5, R=5, T=5, Satisfaction=5, confidence high. The report remains the terminal decision surface; it gains exactly one `Retry verification` action only while the authoritative verified lifecycle is retryable. Ordinary reports retain their exact two actions.
- Causal RED: `HUD.victoryReport.test.ts` failed because no `.st-hud__victory-verified-retry` existed inside the blocking report. GREEN: the retryable state inserts that action before `Play again`, routes it through the existing retry callback, preserves the complete Retry/Play again/Main Menu action ring and focus cycle, remains focused through a repeated retryable HUD refresh, and transfers focus to `Play again` when the real retry transition retires the action to completion-pending. The targeted HUD regression passed 18/18; the main verified-flow plus HUD matrix passed 33/33; full client passed 158 files / 1,533 tests; strict typecheck and diff-check pass. Re-review remains required after the focus-retirement correction. Exact-head hosted CI, Pages deployment, and a new production retry receipt remain required. T-09 remains IN_PROGRESS.

## T-09 complete production retry receipt (2026-08-14)

- Delivery: PR #413 exact reviewed head `08c220ba54cd774b6a82db6104369a91066021fb` cleared CI, CodeQL, Edge, and browser-rendering checks, then merged as `main@ff6b97009ff1dfd91c3ba99ad2e6d945a86d2321`. Its Pages deployment `31764288429` built, verified main provenance, deployed, and passed the post-deploy live smoke. The scope was client/report only, so no Supabase deployment was associated.
- Production receipt: an authenticated Commander at 300/500 Level-2 XP armed the existing diagnostic through its real hit-tested control, closed the panel, completed a fresh six-human/six-CPU Verified Deployment at 26:42 remaining, and observed `Verification needs another attempt.` The blocking After Action Report presented `Retry verification` before `Play again`; its ordinary retry returned the stored result and displayed `Verified victory · +200 XP`. The dossier became R-03 Bombardier, Level 3, 0/500 XP.
- Idempotency proof: reopening the deployed diagnostics surface showed `PASS: identical retry returned the immutable receipt.` with `1 match · 1 win · 200 XP`. This is a completed response-loss/retry proof, not an inference from the UI alone. No result replay or second award is claimed.
- SMARTS selected the user-proposed next slice: a bottom instrumentation rail with a raised arena hard floor, replacing the constrained floating-deck placement. S=5, M=5, A=5, R=5, T=5, Satisfaction=5, confidence high. `career.initiative.0001` remains active.

## Bottom instrumentation rail (2026-08-14)

- The shared engine now reserves y=500 as an immutable arena floor. Terrain deformation, lava, projectile collisions, Sandhog paths, and tank settling stop before the covered band while retaining the 1200 by 600 replay bitmap.
- The fine-pointer Command Deck and liveness companions mount in an explicit bottom rail. The right panel remains the fire-control and roster surface; touch command dock behavior remains overlay-native.
- Geometry is derived from the shared floor and the catalog-derived maximum legal recoil. The opaque rail starts at y=502, strictly below every actual two-, three-, and four-seat rendered 36px chassis pose. Pixel-touch verifies a stalled Leave-to-lobby recovery target remains fully visible and clickable.
- The protected floor alters deterministic projectile and terrain settlement timing. The authoritative Edge verifier's former 198-tick per-turn maximum therefore rejected its reviewed maximum-turn fixture, which now resolves in 207 ticks. The maintainer explicitly authorized the narrow envelope adjustment to 207; the causal boundary test proves 206 remains rejected with `turn_tick_limit`. The total envelope remains 448 ticks, action limits are unchanged, and measured fixture medians remain below the existing 100ms local target.
- Fresh local verification: `npm run check`, strict typecheck, and the exact verified-replay workload/probe Deno suites pass (19/19 Deno tests). Hosted CI, Pages deployment, and live geometry inspection remain required before acceptance.

### Protected-floor browser follow-up (2026-08-14)

- The initial rail placed above the First Salvo coach in stacking order, so a pointer-transparent coach background could no longer pass a fine-pointer drag through to the canvas. The coach now reserves the same derived rail band; its existing real-browser pass-through regression is GREEN across desktop and small fine-pointer views, while its full matrix reports 10 pass / 5 intentional touch-specific skips.
- The y=500 floor legitimately changes the presentation-only terrain bitmap hash used to choose authored worlds. The browser fixtures now record the new deterministic mapping (seed 4 Ember, seed 8 Glassstorm, seed 0 Obsidian; baseline 1337 Obsidian) and use actual page request selection rather than a cache-sensitive response event. Direct asset catalog HTTP/WebP checks and live renderer/fallback visual assertions remain in place. Fresh backdrop/material matrix: 27/27 PASS across desktop, Pixel touch, and compact; strict typecheck and diff-check PASS. Hosted delivery remains outstanding.

### Bottom instrumentation rail delivery (2026-08-14)

- PR #416 exact reviewed head `09d0ee2cf9b5af73a6859ed48684950987e205fe` cleared CI, CodeQL, Edge, and rendering guardrails before squash merge as `main@a237fb64376e213234110509ca1db6b97dfd3fc2`. Exact-main CI `31792840562`, CodeQL `31792840673`, and Pages deployment `31792840612` then passed on that merge.
- The scoped deployments to Supabase project `jdvxfxjpobtyasozxauh` covered `verified_replay_probe` and `complete_verified_deployment`, which both bundle the protected-floor verifier path. No migration or other Edge function changed.
- A fresh production browser tab on the deployed hot-seat First Salvo route measured the coach card ending at y=526.125 while the opaque battle rail begins at y=536.609. The enabled Skip control stayed fully inside the coach card, and the page matched its 1280 by 720 viewport without document overflow. This proves the delivered rail/coaching geometry, not a networked-match result.
- Final local browser follow-up combined First Salvo and terrain/material suites: 37 PASS and 5 intentional touch-specific skips. This supersedes the earlier split 10/5 and 27/27 counts without changing their scope.

## T-13 Live Match Diagnostics local implementation receipt (2026-08-14)

- SMARTS selects a bounded client-side, allowlisted battle snapshot rather than a new Edge probe or a second fault-injection path: S=5, M=5, A=5, R=5, T=5, Satisfaction=5, confidence high. It gives an authenticated operator a practical live-match troubleshooting artifact without a transport request, action, authority change, or match identity exposure.
- RED/GREEN: the pure schema-v1 projector and HUD inspector tests began red before either production surface existed. Main-composition RED then caught the initially incorrect bootstrap-only auth decision: the inspector remained absent after an account authenticated in the already-running tab. The implementation now follows the Lobby account lifecycle and withdraws the provider on sign-out/account change, closing the inspector and removing its trigger.
- Browser RED/GREEN: the first authenticated E2E attempt exposed that the deterministic direct-start route bypasses `Lobby.initialize()` and cannot establish an account. The final production-bundle journey instead installs an isolated scanner-safe test session, waits for the public authenticated Lobby, closes the existing operations modal, starts `Local Battle`, and deploys it. It proves anonymous absence plus authenticated open, allowlisted redaction, clipboard copy, close/focus return, and retained Fire reachability across desktop, compact, and Pixel touch; the touch path exercises the existing Command Menu action. Hosted rendering CI then correctly caught a false touch assertion: opening that existing Command Menu intentionally pauses Fire before the inspector opens. The causal assertion now proves Fire is enabled after normal inspector closure returns to battle, rather than during the intentional pause. Fresh focused browser result: 6/6 PASS; full browser: 303 PASS with 33 intentional profile skips. Focused Vitest result: 24/24 PASS; full client: 160 files / 1,544 tests PASS; strict typecheck and aggregate deterministic/static checks PASS; canonical scanner `[]`; adversarial and coverage final reviews PASS/CLEAR.

### T-13 delivery receipt

- PR #421 exact reviewed head `a5d6398c24ec2b639cee9154ee0f6ff5469720b5` cleared CI, CodeQL, Edge, and browser-rendering checks before squash merge as `main@9cfb497bf3cd4bbb3f80d9fd4993d74b532d4a89`. Exact-main CI `31803227944`, CodeQL `31803227874`, and Pages `31803227878` passed on that merge. This client-only delivery had no Supabase migration, configuration, or Edge-function deployment.
- An authenticated production Chrome session ran the fixed `?diagnostics=1` Pages check and received `Deployed 9cfb497bf3cd · Pages run 31803227878`. It then entered `Local Battle`, deployed the normal hot-seat route, and opened `Inspect live match`. The shown schema-v1 snapshot was `hotseat` / `casual` / `PLAYER_TURN`, round 1, turn 0, Player 1 alive at 100 health, input ready, and `not-applicable` transport; it contained no match identity, account datum, seed, transcript, token, credential, or action payload.
- The live Copy snapshot control was invoked. The Chrome bridge did not return clipboard text, so the receipt does not claim a second live clipboard read; causal production-bundle E2E covers exact copied schema text on desktop, compact, and Pixel touch. Closing the inspector removed its dialog and left the normal Fire Baby Missile control enabled. This proves the released authenticated operator workflow without inferring an online match or a verified-result retry.

## Quick Duel tactical pacing (2026-08-14)

- Causal RED: the default `Quick Duel vs CPU` launch emitted only a fresh seed, so it used the shared engine's intentional one-round fallback. All three human-name/color launch cases failed when the expected configuration required `rounds: 3`.
- GREEN: the Quick Duel route now emits the existing `rounds: 3` option along with its fresh unsigned seed. The shared deterministic match engine owns the resulting first-to-two format, round staging, fresh terrain, carried economy, and clinch; no shared combat values, AI planner, custom hot-seat setting, or online-room contract changed.
- Local evidence: focused Quick Duel launch contract 4/4, strict typecheck, full client 158 files / 1,535 tests, and `npm run check` all pass. Dedicated fixture-free browser coverage causally asserts the visible `Round 1 of 3` contract after launch on desktop, compact, and Pixel touch (3/3). Final adversarial re-clear found zero findings. Exact-head CI, merge, Pages deployment, and a bounded production Quick Duel smoke remain required.

### Delivery receipt

- PR #418 reviewed head `62a88848d5be3a8cab4f08fc0e4df930e9c90cd9` merged as `main@173d7006cb9f710dcf0cd8765c9eb936ae267d46`. Exact-main CI `31796779223`, CodeQL `31796779229`, and Pages `31796779220` are green; Pages completed build, main provenance verification, deployment, and post-deploy live smoke.
- The normal deployed splash and Lobby path entered Quick Duel as Player 1 versus CPU 1. The visible HUD showed `ROUND 1 OF 3`, 0–0 round pips, and both tanks at 100 health. This establishes the released casual-route outcome only; it does not claim an online match or a completed round.
## Battle Command Surface local implementation receipt (2026-08-14)

- SMARTS selected a protected 260px battle band and a three-zone command console over a widened hotkey strip: Specific=5, Measurable=5, Achievable=4, Relevant=5, Time-bounded=4, Satisfaction=5. The player outcome is one coherent decision surface: commander context, a real firing solution, and one honest commitment action.
- RED/GREEN: geometry and browser tests rejected a thin relocated deck, coach overlap, a permanent Store peer, undersized Pixel Skip, side-panel duplication, unsafe 3-player center spawns, and the rendered 36px chassis envelope. GREEN reserves `ARENA_FLOOR_Y=340`, derives the rail boundary from the shared floor/recoil contract, moves Fire/aim/power/wind into the rail, routes Store through Command Menu, and anchors First Salvo above the relevant solution or commitment zone.
- The floor change is a shared-engine compatibility change. Renderer fixtures, deterministic traces, replayed network completion, verified-duel lifecycle, and the Edge replay probe were remeasured rather than loosened. The exact verifier ceilings are 453 total ticks and 221 ticks per turn; causal tests reject 452 and 220. The client diagnostics allowlist now expects the resulting immutable public fixture values.
- Fresh local gates: full client Vitest 163 files / 1,565 tests; strict typecheck; `npm run check`; Edge Deno 352 tests; and the focused 96-case HUD/First Salvo browser matrix passed with its intentional profile skips. `git diff --check` is clean apart from normal CRLF warnings. Exact-diff adversarial review, hosted CI, merge, compatible `verified_replay_probe` and `complete_verified_deployment` deployment, Pages provenance, and fresh hot-seat plus online production journeys remain outstanding; this is not a delivery claim.
- CI correction (2026-08-15): hosted CI reran the existing collapse-animation oracle against the protected-floor geometry and correctly found its former 45°/60 cluster-bomb fixture now converged in one tick. The animation contract remains multi-tick; its deterministic fixture is now the same seed and weapon at 15°/40, which takes ten resolving ticks. Focused `collapse_engine` and a fresh complete `npm run check` pass. This correction is local-only; the reviewed PR head, hosted CI, deployment, and live obligations remain open.
- CI correction (2026-08-15, continued): after that exact head cleared progressive collapse, hosted CI correctly found the old Riot Bomb fall fixture no longer excavated a fall under the protected floor. The replacement seed `0x1a3`, Riot Bomb 35°/100 path proves flight leaves P2 undamaged, resolution drops it 48px, the shared formula yields 24 damage, and a consumed Parachute yields 6. Its lethal branch likewise proves an undamaged tank is killed during resolving rather than blast impact; the safe and live/replay paths remain. Fresh complete `npm run check` passes locally. Hosted review/CI/deploy/live obligations remain open.

## Adaptive Battle Command Console Task 6 local proof (2026-08-15)

- The new causal browser journey drives the ordinary First Salvo briefing and the real protected-rail controls. Hot-seat selects Missile, adjusts live angle and power, spends eight fuel through movement, opens and closes Arsenal with focus return, confirms the existing `G` trajectory guide is current, fires exactly once, observes tracking and impact, retains the renderer-admitted last-salvo correction, and reaches the next commander without a duplicate Fire. The online CPU journey constructs the real `NetworkClient` through Lobby and replaces only Supabase HTTP responses; withholding the Realtime echo forces the existing watchdog to recover the canonical human Fire and the deterministic CPU submission exactly once.
- RED was causal rather than snapshot-only: the focused HUD test failed because the projected impact cue had no rendered/dwelling console surface (1 failed, 16 passed), while the initial compact visual contract reported two physical-readability failures with command text between 4.39 and 5.53 rendered pixels (3 passed, 4 intentionally skipped, 2 failed). The first complete browser run then exposed 46 stale or violated production-browser contracts; correction retained causal public controls and removed no product requirement.
- GREEN on the final candidate: `npm run test:client` passed 163 files / 1,587 tests; `npm run check` passed strict typecheck plus every deterministic/static harness; `npm run check:edge` passed 352/352; standalone `npm run typecheck` passed; and the complete Playwright matrix passed 316 with 38 intentional project-conditional skips and zero failures across 354 cases. `git diff --check` passed with only line-ending notices, and the state-free CodeArbiter secret scan returned `[]`.
- Scope remains presentation and causal browser coverage. No engine, renderer aim-guide algorithm, Auth, action protocol, Supabase function, migration, persistence, dependency, or secret changed. This is local proof only: adversarial/coverage review, exact-head hosted CI and CodeQL, merge, Pages/Supabase deployment, and production hot-seat plus online/CPU evidence remain open and are not claimed.
- Adversarial Task 6 review found one merge blocker in that causal coverage: the new path ended at the second-player handoff while verified terminal recovery remained unit-only. The correction extends the existing production-build verified lifecycle journey across desktop, Pixel touch, and compact profiles. After a failed completion report it proves command mode `recovery`, zero rail-primary Fire controls, exactly one report-owned `Retry verification`, and no report-level Fire; after returning to the Battery, resuming the persisted terminal evidence, and retrying to the accepted immutable receipt, it proves zero Fire and no stale retry action. The focused correction is 3/3 GREEN; the fresh complete browser matrix remains 316 passed / 38 intentional skips / 0 failed, client remains 163 files / 1,587 tests, Edge remains 352/352, and deterministic/static plus standalone typecheck gates pass. This is local correction evidence only; exact-candidate adversarial re-review and every hosted/delivery obligation remain open.
- Hosted CI on exact head `a499659b0846289aa3bbbb23fe1ae2589094d52f` passed typecheck/harness/build, Edge, CodeQL, and CodeRabbit but rejected four rendering cases under its two-worker Linux execution: weapon-intel desktop vertical overflow, compact Pixel weapon-name clipping, and Pixel plus small-window fuel-label escape. The exact hosted metrics were 176 scroll height against a 163 client height, 156 scroll width against a 151 client width, and fuel-label left edges 0.53px/1.64px outside their dial. The causal browser assertions were retained unchanged. The CSS correction gives the two-column dossier 14 authored pixels of vertical headroom without reducing type, stacks compact weapon/ammo metadata inside its bay, and centers a monospace fuel readout across the dial's owned width. The affected two-worker matrix passes 63 with 21 intentional skips; a fresh exact full two-worker matrix passes 316 with 38 intentional skips and zero failures. This is a local correction only; exact-diff adversarial re-review and a new hosted exact-head run remain required.

### Adaptive Battle Command Console delivery and production receipt (2026-08-15)

- PR #425's exact reviewed head `9360c67f7ba9607a7923f46944dbd73c14ed2d16` cleared CI's typecheck/harness/build, Edge, and rendering-E2E jobs plus CodeQL, then squash-merged as `main@b142f26540ec2f27a03f0c11bf7372208f156d18`. Exact-main CI `31870611518`, CodeQL `31870611469`, and Pages `31870611508` all passed. Pages built the client, verified current-main provenance before and during deployment, and passed post-deploy live smoke.
- PR #425 includes the protected-floor verifier envelope but no Auth, persistence, award-authority, migration, configuration, or unrelated Edge-function change. Live Supabase metadata records `verified_replay_probe` v11 and `complete_verified_deployment` v6 with the same `2026-08-15 06:55:15 UTC` update time as the exact-main Pages provenance run. This is a live deployment identity receipt, not a claim about unrelated functions.
- Fresh Pages hot-seat observation traversed Local Battle and the First Salvo entry. The delivered page showed the lower `Turn command console` with Player 1 commander/fuel context, weapon/ammo, Ballistic Computer Angle/Power/Wind, one Fire action, and a separate Match ledger. Firing changed the commitment region to `Tracking shot` / `Shot in flight` rather than retaining Fire.
- Fresh Pages online observation created a private 2/2 operation with one Medium CPU, readied QA, and entered the real deployed lockstep game. The page showed the same lower console and a two-seat turn-order ledger with QA and CPU 1. QA fired one ordinary Baby Missile. The subsequent observed state returned to QA with 40 health while CPU 1 remained at 100, proving the CPU handoff occurred through the live path. No conclusion is drawn about result authority, award, retry, or general network fault recovery from this single observed exchange. The operation was returned to the lobby and left after observation.
- SMARTS closeout: S=5, M=5, A=5, R=5, T=5, Satisfaction=5, confidence high. The selected proof chain is sufficient because it combines exact reviewed CI, exact-main provenance/deployment, a hosted smoke, and direct hot-seat plus online/CPU player journeys while preserving the narrow evidence boundary. `career.initiative.0001` remains active; this closes only the adaptive battle-command console outcome.

## T-14 First Strike local implementation receipt (2026-08-14)

- SMARTS selected a fixed client-only Verified Deployment objective over persistence/rewards or another input tutorial: S=5, M=5, A=5, R=5, T=5, Satisfaction=5, confidence high. It turns the existing deterministic duel into a clear, immediately replayable tactical challenge without creating an entitlement, Auth, database, Edge, or progression authority surface.
- Causal RED/GREEN: the reducer began RED because no First Strike module existed; HUD composition began RED because no objective presentation method existed; main composition began RED because a real recovered two-salvo duel published no objective state. A browser assertion then caught the initial cue mounted beside rather than inside the verified status region. GREEN now keeps a third fire active through flight/resolution, gives a damage result precedence at terminal state, waits for the CPU response before a clean third-shot miss, and mounts the live cue inside the existing verified status region.
- Recovery correctness: replaying a resumed transcript observes each historical settled state before publication, so a first-salvo hit survives a resumed fourth-salvo state. A separate hostile fourth-salvo hit regression refuses to backdate it into First Strike success. The public projection contains only label, remaining salvos, and terminal achieved/missed copy; it exposes no seed, session ID, transcript, account datum, or hidden target fact.
- UI evidence: the authenticated Verified Deployment brief now includes a compact Commander dossier objective, the live status shows `First Strike · Damage CPU in first 3 salvos`, and the existing After Action Report displays terminal result text without adding or reordering actions. Desktop and Pixel browser verified journeys passed 9/9 each, including their existing document no-scroll assertions. Focused Vitest passed 39 tests across reducer, HUD, lobby, and main composition; strict typecheck and aggregate deterministic/static `npm run check` passed. Full-client/compact-browser/adversarial/exact-head/CI/deploy/production gates remain open; no delivery or live result is claimed.
- Review correction (2026-08-14): the prior phrase "waits for the CPU response" was too broad and is superseded. An undamaged third salvo now remains active only through FIRING/RESOLVING, then becomes missed on entry to the CPU turn; this prevents later self/environmental CPU damage from earning false credit. A causal reducer/main regression proves terminal state cannot be changed by that later damage. The exact candidate typechecks again; review/CI/deploy/production gates remain open.
- Attribution correction (2026-08-14): raw current CPU health was not a sufficient success oracle during an early CPU response. The existing verified-duel controller now exposes a read-only, client-local vector of CPU-health deltas captured from each accepted human fire to that same salvo's settlement, before CPU planning/response. It remains outside replay results, transport, Edge Functions, persistence, and awards. RED/GREEN now includes CPU-side health loss during an early response (remains active), terminal/late damage (cannot backdate success), resumed first-salvo success, and controller settlement capture. Focused reducer/controller/HUD/Lobby/main tests pass 83/83 with strict typecheck; full-client/browser/review gates remain open.
- Current local gate receipt (2026-08-14): strict typecheck, `npm run check`, and `git diff --check` pass. Full client Vitest passes 162 files / 1,559 tests. The exact production-build Verified Deployment browser journey passes 27/27 across desktop-fine, pixel-touch, and small-window profiles. Independent adversarial and mutation-oriented reviews cleared the human-salvo attribution correction (C0/H0/M0/L0); both confirm no replay, protocol, Auth, privacy, persistence, award, or CPU-side-damage credit regression. Hosted exact-head CI, merge, Pages deployment, and authenticated production proof remain open.
- T-14 delivery and production receipt (2026-08-14): PR #423 reviewed head `93c10af1f7cddc79d831ab2ca40225a909edc77d` merged as `main@b313876106dd15b5def0dbdc66a7ea77efacbf77`. Exact-head PR CI (typecheck/harness/build, Edge, rendering E2E, CodeQL) passed; exact-main CI and CodeQL are green, and Pages run `31822850852` successfully built, verified current main, and deployed the client. On a fresh signed-in Commander SUaDtL browser tab, the normal Local Battle -> Start verified deployment route showed the production briefing's `First Strike · Damage the CPU within your first three salvos`, then launched a fresh verified deployment at `29:55 remaining` with `You 0 / 6 · CPU 0 / 6` and live `First Strike · Damage CPU in first 3 salvos · 3 salvos remaining`. This proves the released presentation/launch outcome only; it does not claim a full tactical completion or a reward beyond the existing verified path.
## Verified Operations Cycle delivery

PR #427 reviewed head `d8ca33be7791d90492177724e286cdfd759b51df` merged as
`main@6547079dc81e5c8cb27fb9f887d451ca32b0ee37`. Exact-head CI
`31876577759` and CodeQL `31876577763` passed; exact-main CI `31876818163`,
CodeQL `31876818166`, and Pages `31876818184` passed. Pages verified main and
deployed provenance, then completed its live smoke. The implementation is
client-only, so no Supabase function or migration deployment was needed.

The bounded authenticated production journey started Hold the Field, completed
the six-salvo cap, reloaded and resumed the same verified descriptor, received
an immutable verified-loss receipt with the existing +100 XP award, then took
the sole Brief next order action. The Battery showed rotated First Strike, and
a new deployment began with You 0 / 6 and CPU 0 / 6. This verifies the
recovery, receipt, rotation, and fresh-budget loop without claiming a win or
award replay. Verified Operations Cycle is ACCEPTED; `career.initiative.0001`
remains active.
