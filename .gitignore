node_modules
dist
*.log
.DS_Store
client/dist
server/dist
.vite
.env
.env.local
client/.env
client/.env.local
supabase/.env
# Edge Function secrets (hold the SERVICE-ROLE key) — explicit belt-and-suspenders
# entries in addition to the global .env/.env.local catch-all above.
supabase/functions/.env
supabase/functions/.env.local
supabase/.temp/
# Netlify CLI local state (linked-site id + deploy cache) — not secrets, but local.
.netlify
# Generated by `deno check` of the edge functions (local typecheck only).
deno.lock
singedterra-dist-*.zip
# codeArbiter local runtime state (read-inject caches, standup + gate markers) —
# per-working-tree, not shared. The rest of .codearbiter/ (context, reports) is tracked.
.codearbiter/.markers/
